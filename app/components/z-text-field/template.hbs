{{#if hasBlock}}
  {{yield (hash
    input=(component 'z-text-field/trim-input' type=type value=value name=name placeholder=placeholder autocomplete=autocomplete autocapitalize=autocapitalize disabled=disabled maxLength=maxLength)
  )}}
{{else}}
  {{z-text-field/trim-input type=type value=value name=name placeholder=placeholder autocomplete=autocomplete autocapitalize=autocapitalize disabled=disabled maxLength=maxLength}}
{{/if}}
